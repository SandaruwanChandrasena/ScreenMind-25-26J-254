<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===================== -->
    <!-- REQUIRED PERMISSIONS -->
    <!-- ===================== -->

    <!-- Internet (React Native / Firebase) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Usage Stats (Screen usage, app switching) -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Location (Isolation mobility) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- Foreground service (Isolation + Sleep tracking) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- Notifications (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Bluetooth (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Motion / Activity Recognition (Sleep) -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <!-- Microphone (Sleep / snoring detection) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Do Not Disturb access (Sleep) -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- Lock Screen (Social Media) -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />

    <!-- System Overlay (Social Media - draw over other apps) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground Service (Overlay) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- Optional -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- ===================== -->
    <!-- Package visibility (Android 11+) -->
    <!-- ===================== -->
    <queries>
        <package android:name="com.whatsapp" />
        <package android:name="com.instagram.android" />
        <package android:name="com.facebook.katana" />
        <package android:name="com.facebook.orca" />
        <package android:name="com.snapchat.android" />
        <package android:name="com.twitter.android" />
        <package android:name="com.linkedin.android" />
        <package android:name="org.telegram.messenger" />
        <package android:name="com.google.android.youtube" />
        <package android:name="com.zhiliaoapp.musically" />
    </queries>

    <!-- ===================== -->
    <!-- APPLICATION -->
    <!-- ===================== -->

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:supportsRtl="true">

        <!-- ===================== -->
        <!-- Isolation Foreground Location Service -->
        <!-- ===================== -->
        <service
            android:name=".isolation.LocationForegroundService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- Unlock Receiver (Isolation) -->
        <receiver
            android:name=".isolation.UnlockReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.intent.action.SCREEN_ON" />
            </intent-filter>
        </receiver>

        <!-- ===================== -->
        <!-- Notification Listener (Sleep / Notifications module) -->
        <!-- ===================== -->
        <service
            android:name="com.lesimoes.androidnotificationlistener.RNAndroidNotificationListener"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- ===================== -->
        <!-- Overlay Service (Social Media - system overlay) -->
        <service
            android:name=".socialmedia.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse" />

        <!-- Device Admin Receiver (Social Media - Lock Screen) -->
        <!-- ===================== -->
        <receiver
            android:name=".socialmedia.DeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <!-- ===================== -->
        <!-- MAIN ACTIVITY -->
        <!-- ===================== -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

    </application>

</manifest>